---
name: gh-linux

on:
  push:
    paths:
      - make/apps/gh.mk
  workflow_run:
    workflows:
      - Docker images
    types:
      - completed

jobs:
  app:
    uses: ./.github/workflows/common-app.yml
    strategy:
      matrix:
        distro:
          - debian
          - fedora-latest
          - ubuntu-rolling
          - ubuntu-devel
          - ubuntu-24.04
          - ubuntu-26.04
          - opensuse-tumbleweed
        application:
          - app-gh
    with:
      distro: ${{ matrix.distro}}
      application: ${{ matrix.application }}
